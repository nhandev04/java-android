<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" 
     package="com.tranbichlien.finalproject"
    >

    <!-- Permission for notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Permission for internet access to load images from URLs -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.finalproject"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".activity.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.LoginActivity" android:exported="true" />
        <activity android:name=".activity.RegisterActivity" android:exported="true" />
        <activity android:name=".activity.MainActivity" android:exported="true" />
        <activity android:name=".activity.ProductDetailActivity" android:exported="true" />
        <activity android:name=".activity.ProfileConfigActivity" android:exported="true" />
        <activity android:name=".activity.OrdersActivity" android:exported="true" />
        <activity android:name=".activity.AddressActivity" android:exported="true" />
        <activity android:name=".activity.PaymentMethodActivity" android:exported="true" />
        <activity android:name=".activity.CouponsActivity" android:exported="true" />
        <activity android:name=".activity.SettingsActivity" android:exported="true" />

    </application>

</manifest>
